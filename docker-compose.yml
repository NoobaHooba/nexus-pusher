version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - '3001:3001'
    environment:
      - PORT=3001
    volumes:
      - /tmp/nexus-pusher-uploads:/tmp/nexus-pusher-uploads

  frontend:
    build: ./frontend
    ports:
      - '5173:80'
    depends_on:
      - backend
